<section id="la-extension" class="section-shell extension-section" aria-labelledby="extension-title">
	<div class="section-heading center">
		<p class="eyebrow">La extensión</p>
		<h2 id="extension-title" class="section-title">Una puerta académica para San Casimiro y el sur de Aragua.</h2>
		<p class="section-copy">
			La UNEFA Extensión San Casimiro concentra información académica, avisos y orientación institucional para aspirantes, estudiantes, docentes y comunidad local.
		</p>
	</div>
	<div class="extension-grid">
		<article class="card feature-card">
			<h3>Institución pública universitaria</h3>
			<p>El portal conecta la identidad nacional de UNEFA con necesidades concretas de orientación local.</p>
		</article>
		<article class="card feature-card accent">
			<h3>Contenido verificable</h3>
			<p>Las fechas, carreras, contactos y procesos deben publicarse con validación institucional.</p>
		</article>
		<article class="card feature-card">
			<h3>Diseño para estudiantes</h3>
			<p>La navegación prioriza tareas frecuentes: oferta académica, avisos, aspirantes y contacto.</p>
		</article>
	</div>
</section>

<style>
	.extension-grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
	}

	.feature-card {
		padding: var(--space-6);
	}

	.feature-card.accent {
		border-top: 5px solid var(--color-yellow-500);
	}

	h3 {
		margin-bottom: 0.75rem;
		font-size: 1.35rem;
	}

	p {
		color: var(--color-neutral-600);
		font-family: var(--font-reading);
	}

	@media (max-width: 820px) {
		.extension-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
